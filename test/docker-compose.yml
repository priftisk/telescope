services:
  service_a:
    build: ./service_a
    ports:
      - "3333:3333"
    labels:
      - proxy.host=localhost
      - proxy.path=api
  service_b:  
    build: ./service_b
    ports:
      - "4444:4444"
    labels:
      - proxy.host=localhost
      - proxy.port=4444
      # - proxy.path=auth