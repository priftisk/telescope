services:
  service_a:
    build: ./service_a
    ports:
      - "3333:3333"
    labels:
      - proxy.host=localhost
      - proxy.port=3333
      - proxy.path=api
    # labels:
    #   - telescope.port=3333
    #   - telescope.path=api
  service_b:  
    build: ./service_b
    ports:
      - "4444:4444"
    labels:
      - proxy.host=localhost
      - proxy.port=4444
      - proxy.path=auth
    # labels:
    #   - telescope.port=4444
    #   - telescope.path=auth